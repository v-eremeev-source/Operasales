spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/psql-container
    username: postgres
    password: 1
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:/db/changelog/db-changelog-master.yaml
    enabled: true